version: '3.8'

services:

  maildev:
    image: maildev/maildev
    container_name: maildev
    ports:
      - "1080:1080"
      - "1025:1025"

  rcdp-postgres:
    image: postgres:15
    container_name: rcdp-postgres
    environment:
      - POSTGRES_USER=rcdp
      - POSTGRES_PASSWORD=my_secret_password
      - POSTGRES_DB=rda
    ports:
      - "5433:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data
      # Mount project resources so the init script can access the SQL dump
      - ./resources/data/db:/docker-entrypoint-init-resources:ro
      # Mount the conditional init script into the official init directory
      - ./resources/scripts/01-conditional-restore.sh:/docker-entrypoint-initdb.d/01-conditional-restore.sh:ro
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U rcdp -d rda" ]
      interval: 10s
      timeout: 5s
      retries: 5

  rcdp-service:
    build:
      context: .
      dockerfile: Dockerfile
    # Use VERSION env var for the image tag. Falls back to 'latest' if VERSION is not set.
    image: "ekoindarto/rcdp:0.1.6.1-amd64"
    container_name: rcdp-service
    environment:
      - EXPOSE_PORT=24895
      - APP_NAME=RDA Service
      - PYTHONPATH=/home/akmi/rcdp/src
      - BASE_DIR=/home/akmi/rcdp
      - DB_HOST=rcdp-postgres
      - DB_PORT=5432
      - DB_NAME=rda
      - DB_USER=rcdp
      - DB_PASSWORD=my_secret_password
      - DB_URL=postgresql+psycopg2://rcdp:my_secret_password@rcdp-postgres:5432/rda
      - MAIL_HOST=maildev
      - MAIL_PORT=1025
      # point to the pg_dump installed in the image (installed by Dockerfile)
      - PG_DUMP_PATH=/usr/bin/pg_dump
      # VERSION can be passed to docker-compose to tag images; default is 'latest'
      - VERSION=${VERSION:-latest}
    ports:
      - "24895:24895"
    volumes:
      - ./data/db:/home/akmi/rcdp/data/db
      - ./conf:/home/akmi/rcdp/conf
      - ./logs/docker-logs:/home/akmi/rcdp/logs
      - ./resources:/home/akmi/rcdp/resources:ro
    depends_on:
      rcdp-postgres:
        condition: service_healthy
      maildev:
        condition: service_started
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:24895/api/v1/metrics/health"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s

volumes:
  postgres-data: